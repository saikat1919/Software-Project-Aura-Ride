package com.auraride.app.ui.theme

import androidx.compose.ui.graphics.Color

// Locked design tokens from /plan-design-review (safety-forward direction).
// Green (verified) is RESERVED for verification/safety states so it always means "verified."

// Brand
val Plum = Color(0xFF7B1E52)
val PlumSoft = Color(0xFFA94E82)
val PlumDark = Color(0xFFD67AAB) // brand on dark ground

// Neutrals (warm, plum-biased — chosen, not default grey)
val Ink = Color(0xFF1A1420)
val Surface = Color(0xFFFAF7F5)
val SurfaceDark = Color(0xFF1E151C)
val InkDark = Color(0xFFF6EEF3)
val Muted = Color(0xFF7A6B74)
val MutedDark = Color(0xFFB29FAC)
val Line = Color(0xFFE7DDE4)
val LineDark = Color(0xFF3A2C36)
val Chip = Color(0xFFF1E7EE)
val ChipDark = Color(0xFF2C2029)

// Verified / safety (reserved)
val Verified = Color(0xFF1B8A5A)
val VerifiedDark = Color(0xFF4CC48C)
val VerifiedContainer = Color(0xFFE3F1E9)
val VerifiedContainerDark = Color(0xFF173A2B)

// Semantic
val Warn = Color(0xFFB45309)
val ErrorRed = Color(0xFFB3261E)
